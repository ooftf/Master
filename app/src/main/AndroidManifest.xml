<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.master.kit">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TestActivity.SwitchButtonActivity" />
        <activity android:name=".TestActivity.CameraActivity" />
        <activity android:name=".TestActivity.SwitchFristActivity" />
        <activity android:name=".TestActivity.SwitchSecondActivity" />
        <activity android:name=".TestActivity.CalendarActivity" />
        <activity android:name=".TestActivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TestActivity.PageLayoutActivity" />
        <activity android:name=".TestActivity.GesturePasswordActivity" />
        <activity
            android:name=".TestActivity.TouchEventTest.TouchActivity"
            android:label="@string/title_activity_touch"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TestActivity.ViewPagerTest.ViewPagerActivity" />
        <activity
            android:name=".TestActivity.DesignTest.DesignActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TestActivity.ListView.ListViewActivity"></activity>
    </application>

</manifest>